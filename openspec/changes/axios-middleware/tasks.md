## 1. Setup

- [x] 1.1 Install axios in `apps/web`
- [x] 1.2 Create `src/lib/api-client.ts` with axios instance and interceptor

## 2. Refactor Hooks

- [x] 2.1 Replace `fetch` with `api.get` in `useTodos.ts`
- [x] 2.2 Replace `fetch` with `api.post` in `useCreateTodo.ts`
- [x] 2.3 Replace `fetch` with `api.patch` in `useUpdateTodo.ts`
- [x] 2.4 Remove unused `config` imports from hooks that no longer need them
